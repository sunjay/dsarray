on:
  push:
  pull_request:
    branches:
      - master

name: CI

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 1

      - name: build
        run: make

      - name: test
        run: make test
